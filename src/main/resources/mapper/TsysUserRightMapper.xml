<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 用户权限授权表(TSYS_USER_RIGHT) -->
<mapper namespace="cn.tablego.project.springboot.mapper.TsysUserRightMapper">
    <!-- 字段映射 -->
    <resultMap id="tsysUserRightMap" type="cn.tablego.project.springboot.model.TsysUserRight"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        tur.TRANS_CODE, tur.SUB_TRANS_CODE, tur.USER_ID, tur.CREATE_BY, tur.CREATE_DATE, tur.BEGIN_DATE, tur.END_DATE, tur.RIGHT_FLAG, 
        tur.RIGHT_ENABLE
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 根据条件分页查询用户权限授权表列表 -->
    <select id="findTsysUserRightPage" resultMap="tsysUserRightMap">
        SELECT
            <include refid="allColumns" />
        FROM TSYS_USER_RIGHT tur WHERE 1 = 1
    </select>

</mapper>