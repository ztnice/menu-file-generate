<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 角色权限授权表(TSYS_ROLE_RIGHT) -->
<mapper namespace="cn.tablego.project.springboot.mapper.TsysRoleRightMapper">
    <!-- 字段映射 -->
    <resultMap id="tsysRoleRightMap" type="cn.tablego.project.springboot.model.TsysRoleRight"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        trr.TRANS_CODE, trr.SUB_TRANS_CODE, trr.ROLE_CODE, trr.CREATE_BY, trr.CREATE_DATE, trr.BEGIN_DATE, trr.END_DATE, trr.RIGHT_FLAG, 
        trr.RIGHT_ENABLE
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 根据条件分页查询角色权限授权表列表 -->
    <select id="findTsysRoleRightPage" resultMap="tsysRoleRightMap">
        SELECT
            <include refid="allColumns" />
        FROM TSYS_ROLE_RIGHT trr WHERE 1 = 1
    </select>

</mapper>